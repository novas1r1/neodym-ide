describe('ProjectService Testsuite', function () {
	var $scope, $q, FileService, ProjectService;

	beforeEach(module('neodym'));

	beforeEach(inject(function (_FileService_) {
	  // FileService = _FileService_;
	}));

	it('Should exist', inject(function(ProjectService) { //parameter name = service name
		//expect(ProjectService.getSettings).toBeDefined();
	}));

});

  