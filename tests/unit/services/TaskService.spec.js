describe('TaskService Test Suite', function() {
	var TaskService;

	beforeEach(module('neodym'));

	beforeEach(inject(function (_TaskService_) {
		// TaskService = _TaskService_;
	}));

	it('should exist', function () {
		// expect(TaskService).toBeDefined();
	});

});
